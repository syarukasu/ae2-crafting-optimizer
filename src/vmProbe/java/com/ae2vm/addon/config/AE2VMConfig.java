package com.ae2vm.addon.config;

/** Headless log configuration; never packaged or used to spoof VM mod initialization. */
public final class AE2VMConfig {
    public static boolean isDebugLogging() { return false; }
}
