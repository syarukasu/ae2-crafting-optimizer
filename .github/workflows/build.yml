name: Build

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        ae2Variant: [upstream, uelm]
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Java 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v6

      - name: Download Neo ECO compatibility contracts
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p build/compat
          curl --fail --location --retry 3 \
            --output build/compat/neoecoae-20.3.0.jar \
            https://github.com/DancingSnow0517/NeoECOAEExtension/releases/download/20.3.0/neoecoae-20.3.0.jar
          curl --fail --location --retry 3 \
            --output build/compat/neoecoae-20.4.0.jar \
            https://github.com/DancingSnow0517/NeoECOAEExtension/releases/download/20.4.0/neoecoae-20.4.0.jar

      - name: Build
        run: |
          chmod +x gradlew
          ./gradlew clean build --stacktrace \
            -Pae2Variant=${{ matrix.ae2Variant }} \
            -PneoEco20_3Jar=build/compat/neoecoae-20.3.0.jar \
            -PneoEco20_4Jar=build/compat/neoecoae-20.4.0.jar

      - name: Verify 1.20.1 artifact boundary
        shell: bash
        run: |
          set -euo pipefail
          version="$(grep '^mod_version=' gradle.properties | cut -d= -f2)"
          artifact="build/libs/aco${version}_1.20.1.jar"
          test -f "$artifact"
          ! jar tf "$artifact" | grep -q '^META-INF/neoforge.mods.toml$'
          jar tf "$artifact" | grep -q '^META-INF/mods.toml$'
          ! jar tf "$artifact" | grep -q 'aco.*1.21.1'

      - name: Upload jar
        uses: actions/upload-artifact@v6
        with:
          name: aco-1.20.1-${{ matrix.ae2Variant }}-${{ github.sha }}
          path: build/libs/aco*_1.20.1.jar
          if-no-files-found: error
